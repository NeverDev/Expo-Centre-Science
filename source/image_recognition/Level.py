"""



Penser à ajouter les booléen pour le choix de la langue et du niveau


def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Choisir le niveau "



def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Thermique"




def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Thermique + Corrosion "



def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Thermique + Corrosion + Mécanique"



def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Select the level "



def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Thermic"




def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Thermic + Corrosion "



def init_start_buttons(self):
        self.buttonStart = HandButton(0, None, 3, Conf.hand_area_1, Conf.hand_threshold_1)
        self.buttonReset = HandButton(1, None, 1, Conf.hand_area_2, Conf.hand_threshold_2)
        self.buttonStart.daemon = True
        self.buttonReset.daemon = True
        self.buttonStart.start()
        self.buttonReset.start()
        self.buttonReset.title = "Thermic + Corrosion + Mechanic "
"""